<h2>Listing Plans</h2>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for plan <- @plans do %>
    <tr>
      <td><%= plan.name %></td>

      <td class="text-right">
        <span><%= link "Show", to: analysis_plan_path(@conn, :show, plan), class: "btn btn-default btn-xs" %></span>
        <span><%= link "Edit", to: analysis_plan_path(@conn, :edit, plan), class: "btn btn-default btn-xs" %></span>
        <span><%= link "Delete", to: analysis_plan_path(@conn, :delete, plan), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger btn-xs" %></span>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<span><%= link "New Plan", to: analysis_plan_path(@conn, :new, @analysis) %></span>
